// Root component — for this sprint there's only one page: CVGenerator.
// Routing will be added when the Interview Copilot sprint introduces its page.

import CVGenerator from './pages/CVGenerator';

export default function App() {
  return <CVGenerator />;
}
